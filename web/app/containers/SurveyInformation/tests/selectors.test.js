// import { fromJS } from 'immutable';
// import { makeSelectSurveyInformationDomain } from '../selectors';

// const selector = makeSelectSurveyInformationDomain();

describe('makeSelectSurveyInformationDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
