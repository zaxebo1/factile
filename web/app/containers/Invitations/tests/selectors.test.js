// import { fromJS } from 'immutable';
// import { makeSelectInvitationsDomain } from '../selectors';

// const selector = makeSelectInvitationsDomain();

describe('makeSelectInvitationsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
