// import { fromJS } from 'immutable';
// import { makeSelectPreviewDomain } from '../selectors';

// const selector = makeSelectPreviewDomain();

describe('makeSelectPreviewDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
