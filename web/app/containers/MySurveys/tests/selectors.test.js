// import { fromJS } from 'immutable';
// import { makeSelectMySurveysDomain } from '../selectors';

// const selector = makeSelectMySurveysDomain();

describe('makeSelectMySurveysDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
