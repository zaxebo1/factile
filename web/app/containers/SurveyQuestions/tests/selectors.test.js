// import { fromJS } from 'immutable';
// import { makeSelectSurveyQuestionsDomain } from '../selectors';

// const selector = makeSelectSurveyQuestionsDomain();

describe('makeSelectSurveyQuestionsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
